// 정제 로직입니다. 단계 8개, RefineOrchestrator, RefineContext, 자원 포트 인터페이스가 여기 있습니다.
// 외부 서비스 라이브러리를 여기 넣지 않습니다. 카카오나 T맵을 쓰는 코드는 별도 빌드 단위가 맡습니다.
// 근거는 design/07-interface.md 2절 원칙 4와 3절 포트와 어댑터입니다.
dependencies {
    implementation("org.springframework.boot:spring-boot-starter")
}
