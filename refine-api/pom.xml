<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.address.refine.diagnostics</groupId>
    <artifactId>address-refine-diagnostics</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>refine-api</artifactId>
  <name>refine-api</name>
  <description>
    교정과 검출 진입점 둘을 나눠 받고, 전략에 맞는 모양으로 응답을 조립한다.
    실행되는 스프링 부트 앱이 이것이다. 근거는 design/01-scope.md 6-4절이다.
  </description>

  <dependencies>
    <dependency>
      <groupId>com.address.refine.diagnostics</groupId>
      <artifactId>refine-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
