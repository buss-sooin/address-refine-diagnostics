# CLAUDE.md — address-refine-diagnostics

**이 파일은 「이 코드베이스에서 무엇을 어떻게 만드는가」에만 답한다.** 프로젝트를 어떻게 진행하는가는 Claude 프로젝트 「주소정제 솔루션」이 든다. 두 자리를 섞지 않는다.

**기억으로 코드를 짜지 않는다.** 칸 이름·자릿수·코드값·모듈 이름은 아래 「사실을 어디서 확인하나」가 가리키는 파일에서 읽은 뒤에 쓴다.

---

## 무엇을 만드나

**검색과 진단을 결합한 주소정제 솔루션.** 파는 것은 정제된 주소가 아니라 행동 가능한 실패 사유다. 「실패했습니다」가 아니라 「왜 실패했고 무엇을 하면 되는지」를 돌려준다. 이 한 줄이 흐려지는 코드는 짜기 전에 되묻는다.

**덩어리가 둘이고 서로를 부르지 않는다.** DB 생성이 행정안전부 원천 파일을 엔진 자료로 만들고, 주소정제가 그 자료를 읽어 요청에 답한다. 둘이 만나는 자리는 엔진 DB 하나다. 배치가 도는 중에 정제 요청이 들어와도 흐름이 얽히지 않는다.

**정제는 다섯 단계로 흐른다.** 파싱 → 관문 → 표기 가르기 → 갈래 셋과 합치기 → 실재 확인 → 응답이다. 그림은 `design/01-scope.md` 6-1절, 서술은 `design/03-pipeline.md` 2절에 있다.

---

## 넘지 않는 선

### 사람이 지키는 셋

1. **런타임에 LLM이 없다.** 사용자 요청을 처리하는 경로 어디에도 안 들어간다. 표준 주소인지 판정하는 것도, 닮은 이름을 후보로 제안하는 것도, 그 후보가 실재하는지 확인하는 것도 전부 코드가 한다
2. **어디에도 서울을 박지 않는다.** 행정 단계 수도 고정하지 않는다. 지역 값만 늘리면 확장되는 구조여야 한다
3. **없는 규격·자릿수·코드값을 지어내지 않는다.** 설계 문서에 없으면 `[확인 필요: ~]`로 남기고 되묻는다. 추측을 근거처럼 쓰지 않는다

### ArchUnit이 보는 여덟

구조가 어긋난 모습과 그것이 깨는 규칙은 `design/07-interface.md` 6절 「코드를 의도로 검사하는 눈금」에 표로 있다. 같은 표를 여기 옮겨 적지 않는다. 요약하면 갈래 주입 방식 · 단계 간 호출 · 실재 확인 우회 · 버린 조각 기록 · 등급과 사유의 칸 수 · 코어의 카카오 의존 · 허용 오타의 출처 · 정제 컨텍스트의 수명 여덟이다.

**시험이 없다고 눈금이 없는 것은 아니다.** ArchUnit 시험 여덟은 아직 안 짰다. 그전까지도 위 여덟은 지킨다.

---

## 사실을 어디서 확인하나

| 파일 | 답하는 질문 | 반드시 여기서 읽을 것 |
|---|---|---|
| `design/01-scope.md` | 무엇을 만드는 물건인가 | 0절 용어 정본 · 6절 모듈 경계 · 7절 위험 등급표 |
| `design/02-data-model.md` | 데이터가 어떻게 생겼는가 | 6절 물리 데이터 모델. 테이블·칼럼의 정본 |
| `design/03-pipeline.md` | 어떤 차례로 처리하는가 | 0절 안 「모듈 이름」 · 2절 단계별 상세 |
| `design/04-source-data.md` | 원천 파일을 어떻게 넣는가 | 6절 코드값 · 8절 갱신 서비스 흐름 |
| `design/05-expected-results.md` | 맞게 만들었는지 어떻게 아는가 | 5절 케이스 목록 |
| `design/06-source-schema.md` | 원천 자료가 어떤 테이블로 생겼는가 | 2절 자료별 ERD · 5절 전체 관계 ERD |
| `design/07-interface.md` | 책임을 어떻게 나눴는가 | 4절 인터페이스 열넷 · 6절 검사 눈금 |
| `design/standard-dict/*.csv` | 물리명을 무엇으로 짓는가 | 표준용어정의서 · 표준코드정의서 · 사유코드정의서 |

**칸 이름·자릿수·PK는 `06-source-schema.md`에서, 코드값은 `04-source-data.md`에서 확인한다.** 이 둘은 길어서 통째로 읽으면 컨텍스트가 찬다. 필요한 절만 연다.

**설계 문서를 고치지 않는다.** 코딩하며 달라지면 구현본을 옆에 새로 만든다. 이 `design/`은 작업장에서 복사한 사본이라 여기서 고치면 원본과 갈라진다.

---

## 이름

**같은 것을 다른 이름으로 부르지 않는다.** 정본이 셋으로 갈린다.

| 무엇 | 정본 |
|---|---|
| 업무 용어 | `design/01-scope.md` 0절 |
| 모듈 이름 | `design/03-pipeline.md` 0절 안 「모듈 이름」 |
| 빌드 단위 이름 | 이 파일 「빌드 단위 넷」 |
| 물리명 (테이블·칼럼) | `design/standard-dict/표준용어정의서.csv` |

**「모듈」과 「빌드 단위」를 섞지 않는다.** 모듈은 파서·관문·합치기처럼 정제 흐름 안의 자리를 가리키고, 빌드 단위는 `refine-core`처럼 `pom.xml`을 하나씩 가진 폴더를 가리킨다. Maven이 XML 태그로 `<module>`을 쓰지만 그것은 Maven이 정한 이름이라 못 바꾸므로, 글로 쓸 때만 갈라 적는다.

**자바 타입 이름은 아직 후보다.** `design/07-interface.md` 4절이 든 이름을 기본으로 쓰고, 바꿀 이유가 생기면 그 표를 함께 고친다. 말없이 다른 이름을 쓰지 않는다.

**물리명을 사전 없이 짓지 않는다.** 표준단어에 없는 말이 필요하면 그 자리에서 지어 쓰지 말고 되묻는다.

**파일명은 영문 소문자와 하이픈을 쓰고 내용은 한글이다.** 예외는 `design/standard-dict/` 안의 CSV 하나뿐이고, 그것은 사람이 여는 산출물이라 한글 이름을 쓴다.

---

## 기술 셋

| 무엇 | 값 | 왜 |
|---|---|---|
| 자바 | 21 LTS | 자료가 압도적으로 많다. 25로 얻는 것은 문법 편의뿐이다 |
| 빌드 도구 | Maven 3.9.x | Gradle은 판이 바뀔 때 호환이 갈린다. Maven 4는 아직 rc다 |
| Spring Boot | 4.1.x | 3.5가 2026년 6월에 무상 지원이 끝났고 4.0은 2026년 12월에 끝난다. 4.1이 2027년 7월까지 간다 |
| DB | PostgreSQL 18 | 2025년 9월 출시, 2030년 11월 지원 종료. 19는 `.0`이라 안 쓴다 |

**Spring Boot 4는 자바 17을 바닥으로 요구하므로 21과 부딪히지 않는다.**

**Maven 좌표는 `com.address.refine.diagnostics` · `0.1.0-SNAPSHOT`이다.** 자바 패키지도 이 이름을 뿌리로 갈린다.

---

## 검사 명령

| 무엇 | 명령 | 언제 |
|---|---|---|
| 바뀐 빌드 단위만 컴파일 | `mvn -o -pl refine-core compile` | 파일을 고친 직후. `-pl` 뒤는 고친 단위 이름이다 |
| 구조 검사 여덟 | `mvn -o -pl refine-core test -Dtest='Arch*Test'` | 작업을 마칠 때 |
| 전체 빌드와 시험 | `mvn -o clean verify` | 커밋 전 |

**구조 검사 여덟은 아직 없다.** ArchUnit 시험은 구현 준비 15번이 만든다. 그전까지 두 번째 명령은 시험을 하나도 못 찾고 그냥 통과한다.

**`-o`는 오프라인이고 의존성 조회를 건너뛴다.** Maven은 호출마다 JVM이 새로 뜨므로 이 한 글자가 체감으로 붙는다. 의존성을 바꾼 뒤 처음 한 번은 `-o` 없이 돌려 받아 둔다.

**ArchUnit을 편집 단위로 돌리지 않는다.** 클래스를 전부 훑는 검사라 파일 하나 고칠 때마다 돌리면 느리다. 편집 직후에는 컴파일만 보고, 구조는 작업이 끝난 뒤에 본다.

**`tools/`의 생성기 넷은 이 저장소에서 그대로 돌지 않는다.** 재료인 `reference/`가 여기 없다. 표준사전을 다시 찍을 일이 생기면 작업장에서 돌리고 결과만 옮긴다.

---

## 쓸 때 지킬 것

**번호와 기호를 이름 없이 쓰지 않는다.** 주석·커밋 메시지·설명 어디에서도 예외가 없다. 「③에서 처리한다」가 아니라 「세 번째 단계인 후보 실재 확인에서 처리한다」로 쓴다. 번호를 꼭 써야 하면 `A1(필수 단위 표기 누락)`처럼 이름을 함께 적는다.

**동사를 뭉치지 않는다.** 「붙인다」·「연다」·「넘긴다」로 끝내면 무엇을 했는지 알 수 없다. 어느 파일의 무엇을 어떻게 바꿨는지 적는다.

**바꾼 것은 세어서 보고한다.** 「치환 3/3 맞음」처럼 적는다.

**틀렸으면 철회하고 근거와 함께 남긴다.** 조용히 말을 바꾸지 않는다.

말투와 문장 규칙의 정본은 프로젝트의 `writing-style.md`다. 문서는 「~한다」, 채팅은 「~합니다」로 쓴다.

---

## 빌드 단위 넷

```
address-refine-diagnostics/
  pom.xml         부모. 판 번호와 공통 설정만 든다
  db-gen/         원천 파일을 읽어 엔진 자료로 만드는 배치
  refine-core/    정제 로직 전부. 단계 여덟 · 오케스트레이터 · 자원 포트 인터페이스
  refine-api/     교정과 검출 진입점, 응답 조립. 실행되는 부트 앱이 이것이다
```

| 단위 | 무엇을 의존하나 | 왜 이렇게 갈랐나 |
|---|---|---|
| `db-gen` | `refine-core`를 **의존하지 않는다** | 덩어리 둘은 서로를 부르지 않는다. `design/01-scope.md` 6-2절 |
| `refine-core` | 부트 기본만 | 바깥 서비스 라이브러리를 여기 넣지 않는다. `design/07-interface.md` 2절 원칙 4 |
| `refine-api` | `refine-core` | 등급 판정은 코어, 반환 모양은 API. `design/01-scope.md` 6-4절 |

**`refine-core`의 `pom.xml`에 바깥 서비스 라이브러리를 넣지 않는다.** 카카오나 T맵을 쓰는 코드가 생기면 `provider-*` 또는 `adapter-*` 이름의 빌드 단위를 새로 만들고 그 단위가 `refine-core`를 의존한다. 방향이 반대로 서면 코어에서 그 라이브러리를 부를 수 있게 되어 검사 눈금 하나가 통째로 무너진다.

**확장 단위는 필요해질 때 만든다.** 지금은 구현이 없으므로 빈 단위를 미리 세우지 않는다.

## 지금 이 저장소에 있는 것

**자바 코드가 아직 없다.** 빌드 단위 넷의 `pom.xml`과 설계 문서 일곱, 도식 둘, 표준 사전 CSV 아홉, 생성기 넷이 전부다. 자바 패키지·인터페이스·ArchUnit 시험·DDL·회귀 채점기는 앞으로 만든다.

**무엇을 어느 차례로 만드는지는 프로젝트의 `status.md` 「구현 준비」가 든다.** 여기 옮겨 적지 않는다.
