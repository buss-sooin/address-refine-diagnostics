# CLAUDE.md: address-refine-diagnostics

이 파일은 이 코드베이스에서 무엇을 어떻게 만드는지만 다룹니다. 프로젝트를 어떻게 진행하는지는 Claude 프로젝트 「주소정제 솔루션」에서 다룹니다. 두 내용을 한 파일에 섞어 적지 않습니다.

**기억에 의존해 코드를 작성하지 않습니다.** 칼럼 이름, 자릿수, 코드값, 모듈 이름은 아래 「사실을 어디서 확인하는가」에서 안내하는 파일을 확인한 뒤에 작성합니다.

---

## 무엇을 만드는가

검색과 진단을 결합한 주소정제 솔루션을 만듭니다. 이 솔루션이 제공하는 가치는 정제된 주소 자체가 아니라, 사용자가 바로 조치할 수 있는 실패 사유입니다. 「실패했습니다」라고만 응답하지 않고, 왜 실패했는지와 무엇을 하면 되는지를 함께 응답합니다. 이 원칙에서 벗어나는 코드는 작성하기 전에 사용자에게 먼저 확인합니다.

**솔루션은 DB 생성과 주소정제라는 두 구성 요소로 나뉘며, 두 구성 요소는 서로를 호출하지 않습니다.** DB 생성은 행정안전부 원천 파일을 읽어 엔진 자료를 만들고, 주소정제는 그 자료를 읽어 요청에 응답합니다. 두 구성 요소가 함께 쓰는 것은 엔진 DB 하나뿐입니다. 따라서 배치가 실행되는 동안 정제 요청이 들어와도 두 처리 흐름은 서로 영향을 주지 않습니다.

정제는 5단계로 진행되며, 차례대로 맡는 모듈은 `AddressParser` → `AddressGate` → `NotationClassifier` → `AddressBranch` 구현 3개와 `CandidateMerger` → `ExistenceVerifier` → `ResultGrader`입니다. 흐름도는 `design/01-scope.md` 6-1절에, 단계별 설명은 `design/03-pipeline.md` 2절에 있습니다.

---

## 반드시 지킬 원칙

### 사람이 검토해서 지키는 원칙 3가지

1. **런타임에는 LLM을 사용하지 않습니다.** 사용자 요청을 처리하는 경로 어디에도 LLM 호출을 넣지 않습니다. 표준 주소인지 판정하는 일, 유사한 이름을 후보로 제안하는 일, 그 후보가 실제로 존재하는지 확인하는 일은 모두 코드로 처리합니다.
2. 코드 어디에도 서울을 하드코딩하지 않습니다. 행정구역 단계의 수도 고정하지 않습니다. 지역 값을 추가하는 것만으로 확장할 수 있는 구조로 만듭니다.
3. 설계 문서에 없는 규격, 자릿수, 코드값을 임의로 만들어 내지 않습니다. 설계 문서에서 찾을 수 없으면 원천 자료를 직접 열어 확인하고, 그래도 못 닫으면 그 자리에서 사용자에게 묻습니다. 문서에 확인 필요 표시를 남기지 않습니다. 추측한 내용을 근거처럼 제시하지 않습니다.

### ArchUnit으로 검사하는 규칙 8가지

구조가 규칙을 어긴 사례와 그때 위반하는 규칙은 `design/07-interface.md` 6절 「코드의 의도를 검사하는 구조 검사 규칙 8가지」에 표로 정리되어 있으므로 여기에는 옮겨 적지 않습니다. 검사 대상은 `AddressBranch` 주입 방식, 단계 간 호출, `ExistenceVerifier` 우회, 버린 조각 기록, 등급과 사유의 칼럼 수, 코어의 카카오 의존, 허용 오타의 출처, `RefineContext`의 수명 8가지입니다.

ArchUnit 테스트 8개는 아직 작성하지 않았습니다. **테스트가 없는 동안에도 위 규칙 8가지는 지켜야 합니다.**

---

## 사실을 어디서 확인하는가

| 파일 | 답하는 질문 | 반드시 이 파일에서 확인할 내용 |
|---|---|---|
| `design/01-scope.md` | 무엇을 만드는 물건인가 | 0절 용어 정본 · 6절 모듈 경계 · 7절 위험 등급표 |
| `design/02-data-model.md` | 데이터가 어떻게 생겼는가 | 6절 물리 데이터 모델. 테이블과 칼럼의 정본입니다 |
| `design/03-pipeline.md` | 어떤 순서로 처리하는가 | 0절 안의 「모듈 이름」 · 2절 단계별 상세 |
| `design/04-source-data.md` | 원천 파일을 어떻게 적재하는가 | 6절 코드값 · 8절 갱신 서비스 흐름 |
| `design/05-expected-results.md` | 맞게 만들었는지 어떻게 확인하는가 | 5절 케이스 목록 |
| `design/06-source-schema.md` | 원천 자료가 어떤 테이블 구조인가 | 2절 자료별 ERD · 5절 전체 관계 ERD |
| `design/07-interface.md` | 책임을 어떻게 나눴는가 | 4절 인터페이스 15개 · 6절 구조 검사 규칙 8가지 |
| `design/standard-dict/*.csv` | 물리명을 무엇으로 정하는가 | 표준용어정의서 · 표준코드정의서 · 사유코드정의서 |

칼럼 이름, 자릿수, PK는 `06-source-schema.md`에서, 코드값은 `04-source-data.md`에서 확인합니다. 두 파일은 분량이 많아서 전체를 읽으면 컨텍스트가 빠르게 차므로, 필요한 절만 읽습니다.

**설계 문서는 수정하지 않습니다.** 코딩 중에 설계와 달라지는 부분이 생기면 구현본을 별도 파일로 만듭니다. 이 저장소의 `design/`은 작업장에서 복사한 사본이므로, 여기서 수정하면 원본과 내용이 달라집니다.

---

## 이름

**같은 대상을 다른 이름으로 부르지 않습니다.** 이름의 정본은 대상에 따라 아래와 같이 나뉩니다.

| 대상 | 정본 |
|---|---|
| 업무 용어 | `design/01-scope.md` 0절 |
| 모듈 이름 | `design/03-pipeline.md` 0절 안의 「모듈 이름」 |
| 빌드 단위 이름 | 이 파일의 「빌드 단위 4개」 |
| 물리명 (테이블·칼럼) | `design/standard-dict/표준용어정의서.csv` |

「모듈」과 「빌드 단위」는 구분해서 씁니다. 모듈은 `AddressParser`, `AddressGate`, `CandidateMerger`처럼 정제 흐름을 구성하는 처리 단위를 가리키고, 빌드 단위는 `refine-core`처럼 `pom.xml`을 하나씩 가진 폴더를 가리킵니다. Maven은 XML 태그로 `<module>`을 쓰는데, 이 이름은 Maven이 정한 것이라 바꿀 수 없으므로 문서에서만 두 용어를 구분해 적습니다.

자바 타입 이름은 아직 후보입니다. `design/07-interface.md` 4절에 있는 이름을 기본으로 쓰고, 바꿔야 할 이유가 생기면 그 표도 함께 수정합니다. 사용자에게 알리지 않고 다른 이름을 쓰지 않습니다.

물리명은 표준 사전을 확인한 뒤에 정합니다. 표준단어에 없는 말이 필요하면 임의로 만들지 않고 사용자에게 확인합니다.

파일명은 영문 소문자와 하이픈으로 짓고, 내용은 한국어로 작성합니다. 예외는 `design/standard-dict/` 안의 CSV 파일뿐입니다. 이 파일들은 사람이 직접 여는 산출물이므로 한글 이름을 씁니다.

---

## 기술 스택

| 대상 | 버전 | 선택한 이유 |
|---|---|---|
| 자바 | 21 LTS | 참고 자료가 압도적으로 많습니다. 25로 올려서 얻는 것은 문법 편의 기능뿐입니다 |
| 빌드 도구 | Maven 3.9.x | Gradle은 버전이 바뀔 때 호환성이 깨집니다. Maven 4는 아직 RC 단계입니다 |
| Spring Boot | 4.1.x | 3.5는 2026년 6월에 무상 지원이 종료되었고, 4.0은 2026년 12월에 종료됩니다. 4.1은 2027년 7월까지 지원됩니다 |
| DB | PostgreSQL 18 | 2025년 9월에 출시되었고 2030년 11월에 지원이 종료됩니다. 19는 아직 `.0` 버전이라 사용하지 않습니다 |

Spring Boot 4는 자바 17 이상을 요구하므로 자바 21과 충돌하지 않습니다.

**Maven 좌표는 `com.address.refine.diagnostics` · `0.1.0-SNAPSHOT`입니다.** 자바 패키지도 이 이름을 루트로 삼아 구성합니다.

---

## 검사 명령

| 목적 | 명령 | 실행 시점 |
|---|---|---|
| 수정한 빌드 단위만 컴파일 | `mvn -o -pl refine-core compile` | 파일을 수정한 직후. `-pl` 뒤에는 수정한 빌드 단위 이름을 넣습니다 |
| 구조 검사 8가지 | `mvn -o -pl refine-core test -Dtest='Arch*Test'` | 작업을 마칠 때 |
| 전체 빌드와 테스트 | `mvn -o clean verify` | 커밋하기 전 |

구조 검사용 ArchUnit 테스트는 아직 없습니다. 이 테스트는 구조 검사 규칙을 작성하는 15번 작업(프로젝트 `status.md`의 「구현 준비」)에서 만듭니다. 그전까지는 두 번째 명령인 구조 검사를 실행해도 테스트를 하나도 찾지 못한 채 통과합니다.

`-o`는 오프라인 모드로, 원격 저장소에서 의존성을 조회하는 과정을 건너뜁니다. Maven은 호출할 때마다 JVM을 새로 띄우기 때문에 이 옵션 하나로 실행 시간이 눈에 띄게 달라집니다. 의존성을 변경한 뒤 처음 한 번은 `-o` 없이 실행해서 의존성을 내려받아 둡니다.

**ArchUnit은 파일을 수정할 때마다 실행하지 않습니다.** 모든 클래스를 검사하기 때문에 파일 하나를 고칠 때마다 실행하면 느립니다. 수정 직후에는 컴파일 결과만 확인하고, 구조 검사는 작업을 마친 뒤에 실행합니다.

`tools/`에 있는 생성기 4개는 이 저장소에서 그대로 실행되지 않습니다. 생성기가 입력으로 쓰는 `reference/` 폴더가 이 저장소에 없기 때문입니다. 표준 사전을 다시 생성해야 하면 작업장에서 실행하고 결과 파일만 옮겨 옵니다.

---

## 글을 쓸 때 지킬 것

번호나 기호만으로 대상을 가리키지 않습니다. 주석, 커밋 메시지, 설명 모두 예외 없이 적용합니다. 「③에서 처리합니다」가 아니라 「`ExistenceVerifier`에서 처리합니다」처럼 이름으로 씁니다. 번호를 꼭 써야 한다면 `A1(필수 단위 표기 누락)`처럼 이름을 함께 적습니다.

동사 하나로 뭉뚱그려 쓰지 않습니다. 「붙입니다」, 「엽니다」, 「넘깁니다」처럼 끝내면 무엇을 했는지 알 수 없습니다. 어느 파일의 무엇을 어떻게 바꿨는지 구체적으로 적습니다.

변경한 내용은 개수를 세어 보고합니다. 「치환 3/3 맞음」처럼 적습니다.

**틀린 내용은 철회하고, 철회한 근거를 함께 남깁니다.** 사용자에게 알리지 않고 말을 바꾸지 않습니다.

말투와 문장 규칙의 정본은 프로젝트의 `korean-writing-docs.md`와 `korean-writing-chat.md`입니다. 앞의 파일은 문서, 주석, 커밋 메시지에 적용하고, 뒤의 파일은 채팅 답변에 적용합니다. 어미는 두 경우 모두 「~합니다」로 씁니다.

---

## 빌드 단위 4개

```
address-refine-diagnostics/
  pom.xml         부모 POM. 버전 번호와 공통 설정만 담습니다
  db-gen/         원천 파일을 읽어 엔진 자료를 만드는 배치입니다
  refine-core/    정제 로직 전체. 단계 모듈 8개, RefineOrchestrator, 자원 포트 인터페이스를 담습니다
  refine-api/     교정·검출 진입점과 ResponseStrategy 구현을 담습니다. 실제로 실행되는 Spring Boot 앱입니다
```

| 빌드 단위 | 의존 대상 | 이렇게 나눈 이유 |
|---|---|---|
| `db-gen` | `refine-core`를 의존하지 않습니다 | DB 생성과 주소정제는 서로를 호출하지 않습니다. `design/01-scope.md` 6-2절 |
| `refine-core` | Spring Boot 기본 의존성만 | 외부 서비스 라이브러리를 넣지 않습니다. `design/07-interface.md` 2절 원칙 4 |
| `refine-api` | `refine-core` | 등급 판정(`ResultGrader`)은 코어가 맡고, 응답 형식(`ResponseStrategy`)은 API가 맡습니다. `design/01-scope.md` 6-4절 |

**`refine-core`의 `pom.xml`에는 외부 서비스 라이브러리를 추가하지 않습니다.** 카카오나 T맵을 사용하는 코드가 생기면 `provider-*` 또는 `adapter-*` 이름으로 빌드 단위를 새로 만들고, 그 빌드 단위가 `refine-core`를 의존하게 합니다. 의존 방향이 반대가 되면 코어에서 외부 라이브러리를 직접 호출할 수 있게 되어, 코어가 카카오에 의존하지 않는지 확인하는 구조 검사 규칙이 의미를 잃습니다.

확장용 빌드 단위는 실제로 필요해질 때 만듭니다. 지금은 구현이 없으므로 빈 빌드 단위를 미리 만들어 두지 않습니다.

## 지금 이 저장소에 있는 것

**아직 자바 코드는 없습니다.** 지금 있는 것은 빌드 단위 4개의 `pom.xml`, 설계 문서 7개, 도식 2개, 표준 사전 CSV 9개, 생성기 4개가 전부입니다. 자바 패키지, 인터페이스, ArchUnit 테스트, DDL, 회귀 채점기는 앞으로 만듭니다.

무엇을 어떤 순서로 만들지는 프로젝트의 `status.md` 「구현 준비」에 정리되어 있으므로 여기에는 옮겨 적지 않습니다.
